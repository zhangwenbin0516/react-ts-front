/*
 * Filename: d:\workspaces\react-ts-front\src\redux\reducer\userInfo.tsx
 * Path: d:\workspaces\react-ts-front
 * Created Date: Thursday, April 30th 2020, 2:45:04 pm
 * Author: zhangwenbin
 * 
 * Copyright (c) 2020 Your Company
 */

import { StoreState } from '@redux/state';
import { userInfo } from '@redux/actions/userInfo';

export const ReducerUserInfo = (state: userInfo, action: Action): StoreState => {
    switch(action.type) {
        default:
            return StoreState
    }
}

export const ReducerDoMain = (state: DoMain, action: Action): StoreState => {
    switch(action.type) {
        default:
            return StoreState
    }
}